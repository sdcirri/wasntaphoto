[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    real_hibp: allow the test to hit the real HIBP API instead of the mock
filterwarnings =
    ignore::async_lru.AlruCacheLoopResetWarning
